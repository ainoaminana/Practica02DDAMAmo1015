<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Practica02DDAMAmo1015.Views.GaleriaPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Galería">

    <!--
        Página que muestra una galería de animales en formato de cuadrícula.
        Cada tarjeta permite ver los detalles del animal seleccionado.
    -->

    <CollectionView x:Name="AnimalesView"
                    SelectionMode="Single"
                    SelectionChanged="OnAnimalSelected">

        <!-- Diseño en cuadrícula con dos columnas -->
        <CollectionView.ItemsLayout>
            <GridItemsLayout Orientation="Vertical" Span="2" />
        </CollectionView.ItemsLayout>

        <!-- Plantilla visual para cada animal -->
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Padding="10"
                       Margin="10"
                       CornerRadius="18"
                       HasShadow="True"
                       BorderColor="{StaticResource AzulCeleste}"
                       BackgroundColor="White">

                    <!-- Animación al pulsar la tarjeta -->
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnCardTapped" />
                    </Frame.GestureRecognizers>

                    <VerticalStackLayout>
                        <!-- Imagen del animal -->
                        <Image Source="{Binding Imagen}"
                               HeightRequest="120"
                               Aspect="AspectFill" />

                        <!-- Nombre del animal -->
                        <Label Text="{Binding Nombre}"
                               HorizontalOptions="Center"
                               FontAttributes="Bold"
                               TextColor="{StaticResource AzulBoton}" />
                    </VerticalStackLayout>

                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>

</ContentPage>